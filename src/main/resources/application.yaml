spring:
  application:
    name: taskflow
  port:
    server: 8080
  datasource:
    url: jdbc:postgresql://localhost:5432/taskflow
    username: taskflow
    password: taskflow
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 5000